#include <stdio.h>

/**
 * Description: main - The program
 * entry point
 * Return: 0 to indicate success.
 */

int main(void)
{
	/*
	 * use printf to output a
	 * string to the stdout
	 */
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
